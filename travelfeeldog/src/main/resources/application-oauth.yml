spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id : ${Google_Client_Id}
            client-secret: ${Google_Client_Secret}
            scope:
              - email
              - profile
      jwt:
        secret: ${JWT_KEY}
        jws-algorithms: RS512